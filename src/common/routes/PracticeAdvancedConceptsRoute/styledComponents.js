import tw from "tailwind.macro"
import styled from "@emotion/styled"


export const Container = styled.div`${tw`flex flex-col items-center w-screen my-10`}`

export const Header = styled.h1`${tw`text-4xl font-bold items-center `}`